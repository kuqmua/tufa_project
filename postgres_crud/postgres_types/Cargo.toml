[package]
name = "postgres_types"
version = "0.1.0"
edition = "2024"
description = "description"
repository = "repository"
readme = "readme"
license = "license"
keywords = ["keyword"]
categories = ["category"]

[dependencies]
sqlx.workspace = true
uuid.workspace = true
serde.workspace = true
chrono.workspace = true
thiserror.workspace = true
gen_postgres_types = {path = "./gen_postgres_types"}
postgres_types_common = {path = "./postgres_types_common"}
postgres_crud_common = {path = "../postgres_crud_common", features = ["test-utils"]}
error_occurence_lib = {path = "../../error_occurence_lib"}
where_filters = {path = "../where_filters"}

[lints]
workspace = true

[features]
test-utils = ["postgres_crud_common/test-utils"]