# План улучшений проекта tufa_project

## 1. Код и качество

### 1.1 TODO/FIXME/HACK комментарии (115+)
- [ ] Обработать все TODO комментарии в `pg_crud/pg_types/gen_pg_types_src/src/lib.rs`
- [ ] Обработать все TODO комментарии в `pg_crud/pg_json_obj/gen_pg_json_obj_src/src/lib.rs`
- [ ] Обработать все TODO комментарии в `pg_crud/pg_tbl/gen_pg_tbl_src/src/lib.rs`
- [ ] Обработать все TODO комментарии в `pg_crud/pg_json/gen_pg_json_src/src/lib.rs`
- [ ] Обработать все TODO комментарии в `pg_crud/wh_flts/gen_wh_flts_src/src/lib.rs`
- [ ] Обработать FIXME и HACK комментарии во всех крейтах
- [ ] Удалить устаревшие TODO которые уже решены

### 1.2 Закомментированный код
- [x] Проверить и удалить закомментированный код в `gen_pg_types_src/src/lib.rs`
- [x] Проверить и удалить закомментированный код в `gen_pg_json_obj_src/src/lib.rs`
- [x] Проверить и удалить закомментированный код в `gen_pg_tbl_src/src/lib.rs`
- [x] Проверить и удалить закомментированный код в `gen_pg_json_src/src/lib.rs`
- [x] Проверить и удалить закомментированный код в `server/src/`

### 1.3 Переиспользование кода в макросах
- [ ] Выделить общие паттерны DTsBuilder между `pg_types`, `pg_json`, `pg_json_obj`, `pg_tbl`
- [ ] Найти дублирующиеся `quote!{}` блоки для sqlx encode/decode между крейтами
- [ ] Вынести общие генераторы impl блоков в `pg_crud_macros_cmn`
- [ ] Проверить `gen_pg_json_cmn` на полноту — все ли общие паттерны между `pg_json` и `pg_json_obj` вынесены
- [ ] Унифицировать генерацию error enum'ов (serde_er_enum_d_ts_builder используется в 3+ крейтах)
- [ ] Рассмотреть вынос общих DTsBuilder конфигураций в `pg_crud_macros_cmn`

### 1.4 Размер файлов
- [ ] `gen_pg_json_obj_src/src/lib.rs` (~6930 строк) — разбить на модули
- [ ] `gen_pg_types_src/src/lib.rs` (~5645 строк) — разбить на модули
- [ ] `gen_pg_tbl_src/src/lib.rs` (~3800 строк) — разбить на модули
- [ ] `gen_pg_json_src/src/lib.rs` (~3300 строк) — разбить на модули
- [ ] `gen_wh_flts_src/src/lib.rs` — оценить необходимость разбиения

### 1.5 Обработка ошибок
- [x] Проверить все `expect()` вызовы на наличие 8-символьного UUID префикса (все 395 вызовов соответствуют)
- [x] Заменить оставшиеся `unwrap()` на `expect()` с UUID (0 unwrap найдено)
- [ ] Проверить что в library коде нет `expect()`/`panic!()` (только в proc-macro и тестах)
- [ ] Рассмотреть замену `expect()` в proc-macro на `compile_error!()` где возможно для лучших сообщений об ошибках
- [ ] Проверить что все `thiserror` error enum'ы имеют информативные сообщения

## 2. Тестирование (1,758 тестов)

### 2.1 Покрытие тестами
- [ ] Добавить тесты для `pg_crud_macros_cmn` — утилиты макросов
- [ ] Добавить тесты для `pg_crud_cmn` — общие типы
- [ ] Добавить тесты для `pg_crud_cmn_and_macros_cmn`
- [ ] Добавить тесты для `naming` крейтов
- [ ] Добавить тесты для `config_lib` и его подкрейтов
- [ ] Добавить тесты для `loc_lib` — отслеживание ошибок
- [ ] Оценить покрытие edge cases в существующих тестах

### 2.2 Структура тестов
- [ ] Унифицировать паттерн `gen_*_test` / `gen_*_test_cnt` — проверить что все крейты его используют
- [ ] Проверить что тесты запускаются в CI
- [ ] Добавить integration тесты для серверных endpoint'ов
- [ ] Рассмотреть property-based тестирование для генераторов кода

### 2.3 Тестовая инфраструктура
- [ ] Настроить тестовую БД отдельную от dev БД
- [ ] Добавить fixtures/factories для тестовых данных
- [ ] Рассмотреть `sqlx::test` для database тестов с автоматическим rollback

## 3. Зависимости и сборка

### 3.1 Зависимости
- [ ] Аудит workspace зависимостей — убрать неиспользуемые
- [ ] Проверить версии зависимостей на актуальность
- [ ] Запустить `cargo audit` для проверки известных уязвимостей
- [ ] Рассмотреть `cargo-deny` для политики зависимостей
- [ ] Проверить что все зависимости указаны на workspace уровне (нет дублирования версий)

### 3.2 Время компиляции
- [ ] Профилировать время компиляции (`cargo build --timings`)
- [ ] Определить самые тяжёлые крейты и оптимизировать
- [ ] Проверить эффективность `sccache` конфигурации
- [ ] Рассмотреть разбиение тяжёлых proc-macro крейтов для инкрементальной компиляции
- [ ] Оценить влияние `--all-features` на время сборки

### 3.3 Cargo.toml
- [ ] Проверить что все крейты имеют корректные metadata (description, license, etc.)
- [ ] Убрать `publish = false` где не нужно (или добавить где нужно)
- [ ] Проверить feature flags — нет ли неиспользуемых features

## 4. CI/CD

### 4.1 Настройка CI
- [ ] Добавить GitHub Actions workflow (или проверить существующий)
- [ ] CI должен запускать: `cargo fmt --check`, `cargo clippy`, `cargo test`
- [ ] Добавить проверку `cargo doc` в CI
- [ ] Добавить `cargo audit` в CI pipeline
- [ ] Настроить кэширование cargo registry и target в CI

### 4.2 Автоматизация
- [ ] Добавить pre-commit hooks для fmt и clippy
- [ ] Рассмотреть `cargo-make` или `just` для стандартизации команд разработки
- [ ] Автоматическое создание changelog из коммитов

## 5. Docker и деплой

### 5.1 Docker
- [x] Проверить и обновить `docker-compose.yml` в `server/`
- [x] Добавить health checks для PostgreSQL контейнера
- [x] Добавить volume для persistence данных БД (уже был)
- [ ] Рассмотреть multi-stage Docker build для production образа сервера
- [x] Добавить `.dockerignore` для оптимизации контекста сборки (уже был)

### 5.2 Деплой
- [ ] Документировать процесс деплоя
- [ ] Настроить environment-specific конфигурации
- [ ] Рассмотреть контейнеризацию всего приложения

## 6. Сервер и API

### 6.1 Axum сервер
- [ ] Проверить структуру маршрутов — организация по модулям
- [ ] Добавить middleware для логирования запросов
- [ ] Добавить middleware для обработки ошибок (unified error response)
- [ ] Проверить CORS настройки
- [ ] Добавить rate limiting
- [ ] Проверить graceful shutdown

### 6.2 API дизайн
- [ ] Проверить консистентность API endpoints (naming, HTTP methods, status codes)
- [ ] Убедиться что все endpoints документированы через utoipa/OpenAPI
- [ ] Добавить versioning для API (`/api/v1/...`)
- [ ] Проверить pagination во всех list endpoints
- [ ] Добавить валидацию входных данных на границе системы

### 6.3 База данных
- [ ] Проверить все миграции на идемпотентность
- [ ] Добавить индексы для часто используемых WHERE условий
- [ ] Проверить connection pool настройки
- [ ] Рассмотреть prepared statements для частых запросов
- [ ] Добавить мониторинг медленных запросов

## 7. Безопасность

### 7.1 Общее
- [x] Проверить что `.env` файлы не попадают в git (`.gitignore`) — добавлен `telegram_bot/.env`, создан `.env.example`
- [ ] Проверить все endpoint'ы на аутентификацию/авторизацию
- [x] Проверить SQL injection защиту (sqlx параметризованные запросы — используются)
- [x] Проверить что секреты не hardcoded в коде (чисто, кроме dev docker-compose)
- [ ] Рассмотреть использование `secrecy` крейта для sensitive данных

### 7.2 Зависимости
- [ ] Регулярный `cargo audit`
- [ ] Настроить Dependabot или Renovate для автоматического обновления зависимостей

## 8. Документация

### 8.1 Код
- [ ] Добавить doc comments к публичным API в `pg_crud_cmn`
- [ ] Добавить doc comments к публичным API в `loc_lib`
- [ ] Добавить doc comments к ключевым типам в `naming`
- [ ] Добавить примеры использования в doc comments для основных генераторов

### 8.2 Проект
- [ ] Обновить `NAMING.md` если есть новые сокращения
- [ ] Документировать архитектуру кодогенерации (как `gen_*` крейты работают вместе)
- [ ] Документировать процесс добавления нового PostgreSQL типа
- [ ] Документировать процесс добавления нового CRUD endpoint'а

## 9. Производительность

### 9.1 Runtime
- [ ] Профилировать серверные endpoint'ы под нагрузкой
- [ ] Проверить N+1 query проблемы
- [ ] Рассмотреть кэширование для часто читаемых данных
- [ ] Проверить размеры ответов API — нет ли over-fetching

### 9.2 Кодогенерация
- [ ] Профилировать время работы proc-macro'сов
- [ ] Оптимизировать аллокации в `Vec` — использовать `with_capacity` где размер известен
- [ ] Проверить что `TokenStream` конкатенация эффективна

## 10. Архитектура

### 10.1 Структура workspace
- [ ] Оценить необходимость каждого из ~70 крейтов — можно ли объединить мелкие
- [ ] Проверить граф зависимостей между крейтами — нет ли циклических или избыточных
- [ ] Стандартизировать структуру каждого `gen_*` подпроекта
- [ ] Рассмотреть workspace inheritance для общих настроек Cargo.toml

### 10.2 Кодогенерация
- [ ] Рассмотреть альтернативы proc-macro для некоторых генераторов (build.rs, code generation tools)
- [ ] Оценить использование `syn` для парсинга вместо ручного парсинга строк
- [ ] Стандартизировать паттерн генерации кода между всеми `gen_*` крейтами
- [ ] Добавить snapshot тесты для генерируемого кода (`cargo expand` + insta)

### 10.3 Telegram bot
- [ ] Проверить структуру обработчиков команд
- [ ] Добавить error recovery для бота
- [ ] Проверить логирование в боте

## 11. Логирование и наблюдаемость

- [ ] Настроить structured logging (`tracing` крейт)
- [ ] Добавить request ID для трейсинга запросов
- [ ] Настроить уровни логирования по модулям
- [ ] Рассмотреть метрики (prometheus/metrics крейт)
- [ ] Добавить health check endpoint (`/health`, `/ready`)

## 12. Config library

- [ ] Проверить валидацию конфигурации при старте
- [ ] Добавить default значения где уместно
- [ ] Документировать все переменные окружения
- [ ] Рассмотреть hot-reload конфигурации для некритичных параметров
